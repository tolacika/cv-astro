---
export const partial = true;

import { createLlmInput, generateLlmPromptApply } from "../lib/generator";

const input = await createLlmInput();

const body = generateLlmPromptApply(input);
---

{body}