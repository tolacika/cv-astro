---
export const partial = true;

import { createLlmInput, generateLlmPromptDev } from "../lib/generator";

const input = await createLlmInput();

const body = generateLlmPromptDev(input);
---

{body}