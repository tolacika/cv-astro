---
import type { Content } from "../lib/content";
import PostList from "./PostList.astro";

const { content } = Astro.props as { content: Content };

const posts = [] as Array<{href:string,image:string,cta:string,title:string,teaser:string}>;// content.posts.filter(i => i.type == "project" && i.featured);
---

<PostList
  title={content.perspective.title}
  subTitle={content.perspective.subTitle}
  posts={posts}
/>